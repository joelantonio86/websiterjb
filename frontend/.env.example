# Copie para .env.local (nunca commits .env.local)
#
# Localhost — aponta para o backend local (sem Cloud Run):
VITE_API_BASE=http://127.0.0.1:8080

# Produção (só se quiseres testar o front contra a API já deployada):
# VITE_API_BASE=https://rjb-email-sender-215755766100.europe-west1.run.app

# Pasta no R2 com PDFs consolidados por naipe (pré-gerados)
# VITE_R2_CONSOLIDADO_FOLDER=consolidado_naipe

# VITE_SITE_URL=https://www.racionaljazzband.com.br
